var app = app || {};

(function (app) {
	app.validateData = function (object) {
		for (var key in object) {
			if (object.hasOwnProperty(key)) {
				if (object[key]) {
					
				}
			}
		}
	}

})(app);